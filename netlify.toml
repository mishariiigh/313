[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NETLIFY_USE_YARN = "false"
  SKIP_GOOGLE_CHROME_DOWNLOAD = "true"

[[plugins]]
  package = "netlify-plugin-inline-functions-env"
  inputs = {}